# Development dependencies
ruff>=0.4.0
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
isort>=5.12.0

# Local recall engine (dev/CI). In the shipped app these are auto-installed on
# first use via runtime_deps, not bundled — see SAFEGUARDED_PACKAGES.
fastembed>=0.3.0
sqlite-vec>=0.1.6

# OpenAI — dev-only, used by scripts/resummarize_vault.py --provider openai
# (one-off v2 corpus migration). Never imported by the shipped app.
openai>=1.40






