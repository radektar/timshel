# Audyt pełny — sierpień 2026 (przed paczką H1)

Status: FAZA A ZAKOŃCZONA (2026-08-07) — 8 torów + weryfikacja adwersaryjna.
95 znalezisk, po weryfikacji: 2 SHIP-BLOCKERY + 1 na granicy, ~24 FIX-PRZED-H1,
2 FIX-PRZED-GA, ~35 DŁUG-PO-H1. Synteza i rekomendacja fazy B na końcu pliku.
Plan: pełny audyt wszystkich warstw → naprawa znalezisk → paczka testerska.
Klasyfikacja znalezisk: `SHIP-BLOCKER` / `FIX-PRZED-H1` / `DŁUG-PO-H1`.

## A7a — Pomiary (haiku, mechaniczne) ✅

### Churn × LOC × fix-rate (od 2026-05-01)

| Plik | Commity | LOC | Fix-rate |
|---|---|---|---|
| ui/dashboard_window.py | 50 | 4433 | 46% |
| menu_app.py | 56 | 2997 | 41% |
| transcriber.py | 33 | 3608 | 36% |
| connections/scheduler.py | 30 | 1280 | **57%** |
| connections/candidate_assembly.py | 28 | 1046 | 50% |
| ui/settings_window.py | 16 | 1072 | 50% |
| config/config.py | 29 | 564 | 14% |
| summarizer.py | 17 | 881 | 41% |
| setup/wizard.py | 11 | 1326 | 36% |
| setup/downloader.py | 8 | 921 | **63%** |
| connections/recall/seam.py | 11 | 348 | **73%** |
| file_monitor.py | 7 | 501 | **71%** |

18 plików z fix-rate > 50%. Hipoteza H5 („6 największych plików boli") —
wynik niejednoznaczny: wielkość × churn wskazuje dashboard/menu_app/transcriber,
ale **fix-rate** (miara „wracamy naprawiać") wskazuje co innego:
`seam.py` 73%, `file_monitor.py` 71%, `downloader.py` 63%, `scheduler.py` 57%.

### Coverage (fast suite, 76% ogółem, 1577 passed / 148 s)

| Moduł | Coverage |
|---|---|
| config/settings.py | 100% |
| config/config.py | 95% |
| scheduler.py | 89% |
| transcriber.py | 80% |
| vault_index.py | 80% |
| downloader.py | 74% |
| wizard.py | **59%** |
| menu_app.py | **38%** |
| app_core.py | **50%** |
| main.py / startup_manager.py | **0%** |
| onboarding_window.py | **16%** |

### Tempo (od 2026-06-01)

305 commitów, 91 fix (30%), 4,5 commita/dzień.

Surowe dane: scratchpad `METRICS_REPORT.txt`, `coverage.json`.

## A7b — Build empirycznie (sonnet) ✅

| Hipoteza | Werdykt |
|---|---|
| Świeży build → poprawna pieczęć ad-hoc | ✅ `codesign --verify --strict --deep` exit 0 (4125 plików pod pieczęcią) |
| Recall w bundlu | ⚠️ **LEXICAL-ONLY** — fastembed/sqlite_vec/onnxruntime SĄ w bundlu (python312.zip), ale **numpy nie jest bundlowany wcale** → realny import pada, zaprojektowana degradacja dense→lexical działa czysto (log: „falling back to lexical-only", zero ERROR). Tester dostaje ⌃⌥Spację bez warstwy semantycznej. |
| Smoke czysty HOME | ✅ PASS (proces żyje, stamp OK, 0 ERROR/Traceback) |
| Determinizm buildu | ✅ 4131 = 4131 plików, 0 różnic w ścieżkach; python312.zip ±218 B (metadane .pyc) |
| `make clean` niszczy pieczęć | ✅ POTWIERDZONE dry-runem: `Makefile:138` `find .` łapie **115 katalogów __pycache__ wewnątrz dist/Timshel.app** |
| Wersja/stamp | beta.17, stamp `dbbea90` (HEAD), TesterBuild=false (build bez TESTER_BUILD=1) — poprawnie |

Znalezisko A7b-1 (FIX-PRZED-H1): **numpy poza bundlem = martwa warstwa
semantyczna recall u każdego testera**; decyzja: dodać numpy do bundla
(rozmiar!) albo urealnić copy onboardingu (⌃⌥Spacja = tryb dosłowny).
Znalezisko A7b-2 (FIX-PRZED-H1): `make clean` wchodzi do dist/ — naprawa w B2.
Efekt uboczny: `build_app.sh` skasował lokalny DMG 1bbee1af (kopia na iCloud
zostaje); w dist/ leży teraz świeży build dev (nie-tester).

## A1 — Warstwa stanu i współbieżność (opus) ✅

**Korekta założenia:** wysyłany LaunchAgent (`com.timshel.app`) uruchamia bundle
apki menu, nie `main.py` — u testera z DMG jest **jeden proces**. Wyścigi
cross-proces spadają na dev-setup; rośnie waga wyścigów między wątkami.

**Werdykt tezy: potwierdzona częściowo.** Warstwa stanu nie rodzi bugów, ale je
utrwala i przenosi: wszystkie 7 plików stanu ma tę samą wadę projektową —
**każdy czytelnik połyka własny błąd dekodowania i zwraca default**
(`settings.py:166`, `vault_index.py:51`, `scheduler.py:212`,
`usage_ledger.py:104`, `voice_memos.py:180`, `transcriber.py:1414`). Utracony
zapis nigdy nie ujawnia się jako błąd — wraca warstwy dalej jako objaw.

20 znalezisk: **1 SHIP-BLOCKER, 12 FIX-PRZED-H1, 7 DŁUG-PO-H1.**
ProcessLock: 0 defektów (dowód poprawności — flock jądra, stale lock niemożliwy).

| ID | Sev | Gdzie | Co | Klasa |
|---|---|---|---|---|
| A1-20 | HIGH | transcriber.py:651,662 + app_core.py:316 | Tester kasuje słabą notę w Obsidianie → `lookup_by_filename_size` dalej pomija plik; `reconcile` chodzi tylko przy starcie → **nota nie odtwarza się nigdy** (do restartu nazajutrz) | **SHIP-BLOCKER** |
| A1-1 | HIGH | settings.py:174 | `config.json` zapis nieatomowy; ucięty plik → `load()` zwraca defaulty → **kreator od nowa, zgubiony klucz API** | FIX-PRZED-H1 |
| A1-2 | HIGH | settings.py:15,186 | Lock chroni tylko `mutate()`; bare load→modify→save w 14 miejscach — pełny zapis `to_dict()` gubi pola z `mutate` | FIX-PRZED-H1 |
| A1-8 | HIGH | vault_index.py:41–54 | Czytelnicy serwują pamięciowy snapshot odświeżany tylko przy zapisie; drugi proces → podwójna transkrypcja | FIX-PRZED-H1 |
| A1-9 | HIGH | vault_index.py:36–38 | `index.json` + flock **wewnątrz vaultu w iCloud** — flock nie koordynuje między Makami, `os.replace` maksymalizuje konflikt iCloud; multi-Mac = last-writer-wins → podwójne transkrypcje | FIX-PRZED-H1 |
| A1-11 | HIGH | scheduler.py:236,338 | `_merge_disk_seen→_save` bez locka wątkowego; `unsee` z wątku Voice Memos w trakcie płatnego digestu → zapłacone okno wraca jako „unseen" → **podwójny digest Opus** | FIX-PRZED-H1 |
| A1-12 | HIGH | scheduler.py:346 vs 268 | `sorted()` na mutowanym secie → RuntimeError ucieka z `mark_ran` → płatny przebieg nieodnotowany → znów podwójna płatność | FIX-PRZED-H1 |
| A1-18 | MED | transcriber.py:1438–1462 | `_persist_gpu_disabled` RMW bez locka, 2 procesy → próg 2 awarii nieosiągalny → wieczny podwójny przebieg na zepsutym GPU (rodzina objawu #104) | FIX-PRZED-H1 |
| A1-3/4 | MED | wizard.py:59; menu_app.py:1106 | Kreator/review-modali trzymają snapshot settings minutami i zapisują całość na końcu; review omija guard `_PROMPTS_IN_FLIGHT` | FIX-PRZED-H1 |
| A1-6 | MED | state_manager + menu_app:2529 | `last_sync` write-only; „Reset transcription memory" w menu **nie robi nic** — tester użyje i zaraportuje „nie działa" | FIX-PRZED-H1 |
| A1-10 | MED | transcriber.py:2643 | `reconcile` snapshot poza lockiem → może skasować notę właśnie powstającą (tylko 2 procesy) | FIX-PRZED-H1 |
| A1-13 | MED | scheduler.py:531 | `new_notes` — jedyne pole nieadoptowane z dysku w merge; cross-proces zeruje backlog | FIX-PRZED-H1 |
| A1-5/7/14/15/16/17/19 | LOW | — | flock na config (dev), martwy `.backup`, RMW `new_notes+=`, `_hold_owned`, ledger OK, komentarz voice_memos nieprawdziwy, `_clear_gpu_verdict` | DŁUG-PO-H1 |

Najtańsze trzy o największym zwrocie: atomowy zapis `config.json` (~3 linie),
jeden `threading.RLock` w `DigestScheduler` (zamyka A1-11/12/14),
sprawdzenie istnienia MD przy pomijaniu w `find_pending` (A1-20).

## A2 — Ścieżka krytyczna transkrypcji (opus) ✅

21 znalezisk: **2 SHIP-BLOCKERY, 11 FIX-PRZED-H1, 8 DŁUG-PO-H1.**
Stall-detection i pętla streamingu whisper: czyste (finally domyka każdą ścieżkę).

| ID | Sev | Gdzie | Co | Klasa |
|---|---|---|---|---|
| A2-01 | HIGH | transcriber.py:2567,2638 | `reconcile` skanuje vault **niererkursywnie**, a Etap B kasuje z indeksu każdy fingerprint bez trafienia → user przenosi noty do podfolderu → restart → **pełna retranskrypcja historii + duplikaty + ponowny koszt AI**, przy każdym starcie | **SHIP-BLOCKER** |
| A2-02 | HIGH | transcriber.py:690-751 | Staging `recordings/` **nigdy nie jest sprzątany** — każde nagranie zostaje jako kopia; 3 tyg. × 1h WAV = kilkanaście GB + mnożnik skutków A2-01 | **SHIP-BLOCKER** |
| A2-03 | HIGH | transcriber.py:733 | Staging kopiuje prosto na finalną nazwę (bez tmp+rename) → USB wyjęte w trakcie → **obcięty plik transkrybowany jako kompletna nota**, po ponownym wpięciu duplikat | FIX-PRZED-H1 |
| A2-04 | HIGH | transcriber.py:2332,3004 | Porażka postprocessu bez backoffu → co 30 s **kolejne płatne wywołanie Haiku** na 3h transkrypcie (dysk pełny = pętla kosztów) | FIX-PRZED-H1 |
| A2-05 | MED+ | transcriber.py:1750,2408 | Timeout w retry aliasowym (~40% spotkań) wyrzuca **już opłacone** podsumowanie → nota z placeholderem, koszt zapisany | FIX-PRZED-H1 |
| A2-06 | MED | transcriber.py:2169 vs 2912 | Fallback `{stem}*.txt` łapie plik kwarantanny `.stale-*` → adopcja **cudzego transkryptu** (dokładnie awaria, dla której zbudowano sidecar) | FIX-PRZED-H1 |
| A2-07 | MED | transcriber.py:3018-3033 | Ścieżka crash-recovery (TXT istnieje → markdown) nie odpala `_post_note_hooks` → nota niewidoczna dla digestu | FIX-PRZED-H1 |
| A2-08 | MED | vault_index.py:51,177 | Nieczytelny index.json → `_data={}` + natychmiastowy `_save()` **nadpisuje historię dedupu** (jeden EIO z iCloud = wszystko do retranskrypcji) | FIX-PRZED-H1 |
| A2-09 | MED | vault_index.py:184 ← transcriber.py:3086 | `_save()` bez obsługi błędów, poza try → OSError przerywa całą partię, nota osierocona | FIX-PRZED-H1 |
| A2-10 | MED | transcriber.py:2586 vs 2646 | Asymetria reconcile: brak `source` we frontmatterze = kasacja wpisu (klasa A2-01 per nota) | FIX-PRZED-H1 |
| A2-11 | MED | summarizer.py:401 + transcriber.py:2394 | Błąd nietrwały → fallback, ale `summarized_by_llm=True` → placeholder na stałe, bez ścieżki „ponów podsumowanie" | FIX-PRZED-H1 |
| A2-12 | MED | transcriber.py:1526 | WAV 16k (~350 MB dla 3h) zapisywany **do vaulta w iCloud** w trakcie transkrypcji; po SIGKILL zostaje | FIX-PRZED-H1 |
| A2-13 | MED | volume_identity.py:44 | Timeout diskutil → efemeryczny id `fallback:name:` → zaufany dyktafon „nieznany", `blocked` śmieci ustawienia | FIX-PRZED-H1 |
| A2-14..21 | MED/LOW | — | m.in. `register_new_notes` przed try (utracony `unsee` na zawsze), synchroniczny `process_recorder` blokuje digest/skan na 3h partii, `summary_coverage` przy fallbacku, `.stale-*` bez retencji, iterdir bez try w callbacku FSEvents | DŁUG-PO-H1 |

## A3 — Setup + downloader + bootstrap (sonnet) ✅

11 znalezisk: **2 SHIP-BLOCKERY, 6 FIX-PRZED-H1, 3 DŁUG-PO-H1.**
`bootstrap.py` czysty — migracja idempotentna, częściowa awaria obsłużona.

| ID | Sev | Gdzie | Co | Klasa |
|---|---|---|---|---|
| A3-1 | HIGH | downloader.py:231-293 | Pół-rozpakowany encoder Core ML (pełny dysk/kill w `extractall`) liczy się **na zawsze** jako zainstalowany (`exists()`); „Re-download dependencies" nie naprawia, bo `status().ready=True` | **SHIP-BLOCKER** |
| A3-8 | HIGH | permissions.py:9-28 | Detekcja FDA testuje tylko `~/Library/Mail` i `~/Library/Safari` — na świeżym koncie (nieotwarty Mail) oba nie istnieją → `True` bez FDA → **wizard pomija ekran uprawnień**, dyktafon milczy bez wyjaśnienia | **SHIP-BLOCKER** |
| A3-2 | HIGH | dependency_manager.py:59-96 | „Repair" weryfikuje tylko whisper-cli; ffmpeg i model nigdy nie są re-weryfikowane (`check_all()` z checksumami = martwy kod) | FIX-PRZED-H1 |
| A3-6 | MED | wizard.py:100-318 | Download w tle + `_persist_stage` na starcie kroku → kill w trakcie pobierania → resume **omija ekran DOWNLOAD**, wizard może dojść do FINISH bez zależności; bezpiecznik `_check_dependencies` odpala się dopiero przy następnym starcie | FIX-PRZED-H1 |
| A3-9 | HIGH | runtime_deps.py + setup_app.py | Recall dense martwy w bundlu u każdego testera (patrz A7b: fastembed JEST w zipie, brakuje **numpy**; mechanizm inny niż w tym torze, skutek ten sam), cicho, log na DEBUG | FIX-PRZED-H1 (scalone z A7b-1) |
| A3-3 | MED | downloader.py:527,652 | ENOSPC w trakcie zapisu chunku → 3 bezsensowne retry + surowy komunikat | FIX-PRZED-H1 |
| A3-4 | MED | downloader.py:82-96 | Detekcja sieci = UDP/53 do 8.8.8.8 → firmowy VPN/proxy = fałszywe „No internet", ekran DOWNLOAD zablokowany | FIX-PRZED-H1 |
| A3-5 | MED | checksums.py:30-31 | URL-e deps na starym repo `radektar/malinche` — redirect działa, ale przejęcie nazwy = trwała awaria instalacji u wszystkich | FIX-PRZED-H1 |
| A3-7/10/11 | LOW | — | Re-run wizarda pyta znów o Voice Memos po „Skip"; sqlite-vec niepinowany (dev); martwa ścieżka „bundled" whisper (~150 linii) | DŁUG-PO-H1 |

## A5 — Warstwa UI (sonnet) ✅

12 znalezisk unikalnych; A5-1/2/3 to trzy triggery JEDNEGO mechanizmu — bare
`load()` → długie okno modala → `save()` całego snapshotu zamiast `mutate()`
(ten sam root cause co A1-2; jedna poprawka zamyka wszystkie trzy).

| ID | Sev | Gdzie | Co | Klasa |
|---|---|---|---|---|
| A5-1 | HIGH | settings_window.py:1010,1040 + menu_app.py | Follow-up F POTWIERDZONY z mechanizmem: Save w Ustawieniach wskrzesza `pending_import_dir` skonsumowany przez `mutate()` daemona → **drugi import + drugi płatny digest** przy następnym starcie | **SHIP-BLOCKER** |
| A5-2 | HIGH | settings_window + menu_app.py:1103,1156 | Wariant deterministyczny bez wyścigu: „Review mounted disks" zapisuje od razu, otwarte Ustawienia nadpisują starym snapshotem → **zablokowany dysk się odblokowuje** | **SHIP-BLOCKER** |
| A5-3 | HIGH | menu_app.py:1510 vs settings_window | Spóźniona decyzja o dysku (po timeout 600 s) kasowana przez Save w Ustawieniach | **SHIP-BLOCKER** |
| A5-6 | HIGH | dashboard_window.py:4164-4188 | Nieudany handoff (AppleScript/TCC) raportowany jako sukces — `retag(KEPT)` + toast „Przekazano" bezwarunkowo; **zadanie nie powstało, insight znika z Nowych** | **SHIP-BLOCKER** |
| A5-4 | MED→HIGH | menu_app.py:514,639 | Import folderu bez ostrzeżenia o koszcie (każdy plik = płatne summary; setki plików = niekontrolowany wydatek) — w kontrze do digestu, który cenę deklaruje | FIX-PRZED-H1 |
| A5-5 | MED | menu_app.py:2525 | „Reset transcription memory" bez potwierdzenia i wyceny (a wg A1-6 w ogóle nie działa — do scalenia decyzją) | FIX-PRZED-H1 |
| A5-7 | MED | dashboard_window.py:4029 | Handoff bez debounce — dwuklik = dwa zadania w Reminders | FIX-PRZED-H1 |
| A5-8 | MED | dashboard_window.py:4112 vs 4385 | Handoff trzyma indeks insightu; podmiana decka w trakcie → KEPT na złym insightcie | FIX-PRZED-H1 |
| A5-10 | MED | ui/native_alert.py:47-73 | Globalny `alert()` bez hopu na main thread — call sites z wątków tła ryzykują crash AppKit (~70 miejsc niezweryfikowanych) | FIX-PRZED-H1 |
| A5-9/11/12 | LOW | — | `record_action` połyka błąd zapisu (decyzja wraca do „Nowe"); TOCTOU w feedback_export (obsłużony); martwa akcja delegata | DŁUG-PO-H1 |

Pozytywy: zip feedbacku **nie zawiera** `config.json` → brak wycieku klucza API.

## A6 — Bramki mówią prawdę (sonnet) ✅

12 znalezisk: **0 SHIP-BLOCKER, 6 FIX-PRZED-H1, 6 DŁUG-PO-H1.**

**Główny deliverable — mypy bez grandfather listy:** 135 błędów w 22 plikach,
po ręcznej weryfikacji **ZERO realnych bugów** — wszystkie to szum PyObjC,
brak zawężania `self.attr`, kłamiące adnotacje dictów, luki w stubach.
Grandfather list opisuje siebie uczciwie. ALE mechanizm `ignore_errors=true`
wyłącza mypy CAŁKOWICIE dla rdzenia (transcriber, menu_app, summarizer, wizard,
config) — każdy NOWY bug przejdzie zielono (A6-1, FIX-PRZED-H1; lek:
per-error-code `disable_error_code` zamiast pełnego ignore).

**Wzorzec toru:** bramka przechodzi zielono, nic nie zweryfikowawszy:
| ID | Gdzie | Co | Klasa |
|---|---|---|---|
| A6-5 | tests/e2e/*_quality.py | Martwy klucz z .env → 401 → cichy skip zamiast FAIL; „skip" czyta się jako „brak środowiska", znaczy „klucz zepsuty i nikt nie wie". Propozycja uczciwego gate'u w raporcie źródłowym | FIX-PRZED-H1 |
| A6-6 | tests/integration/test_download_automated.py | Bez markera → BIEGNIE na CI: pobiera ~466 MB z sieci na każdy run i **nie ma ani jednego asserta** — nie może być czerwony | FIX-PRZED-H1 |
| A6-7 | test_m5_corrupted_file.py | To samo + mutuje whisper-cli na dysku (w fake HOME) | FIX-PRZED-H1 |
| A6-8 | test_downloader.py:393-523 | Deklaracja „używamy testów integracyjnych" NIEPRAWDZIWA — retry po 500, wyczerpanie MAX_RETRIES, progress_callback, nagłówek Range: **niepokryte nigdzie** | FIX-PRZED-H1 |
| A6-9 | test_recall_{vector_store,engine,retriever} | 22 testy deterministycznie nigdy nie biegną na CI (setup-python bez loadable sqlite extensions) — silnik wektorowy recall de facto niesprawdzany przez pipeline | FIX-PRZED-H1 |
| A6-10..12 | — | ffmpeg nie instalowany na CI (kaskada skipów audio_factory); flake8 poza CI (123/130 = szum, realne: F401×7 dashboard, F811 config.py:17 samo-import); tests/pro = spec sprzed pivotu PRO | DŁUG-PO-H1 |

Korekta pomiaru: isort 28 plików (nie 53 z wcześniejszego rozpoznania).

## A4 — Warstwa AI + connections + recall (opus) ✅

20 znalezisk: **3 SHIP-BLOCKERY, 10 FIX-PRZED-H1, 7 DŁUG-PO-H1.**
Niedokończone: kontrakt kanałów `candidate_assembly.py` (zlecony osobno).

| ID | Sev | Gdzie | Co | Klasa |
|---|---|---|---|---|
| A4-1 | KRYT | scheduler.py:236-306 vs 344 | `new_notes` — jedyne pole bez reguły merge; `unsee()`/`suspend` z drugiego uchwytu zeruje backlog → `is_due()` False → **digest nie odpala się nigdy** (pokrywa się z A1-13, tu z pełnym trace) | **SHIP-BLOCKER** |
| A4-9 | KRYT | config.py:480-489 | `tester_mode` to INNY PRODUKT: verdict + 4 kanały + metryki + Opus 5 na syntezę — domyślka wysyłkowa nie ma nic z tego → **wszystkie pomiary H1 i model kosztowy opisują build, którego nikt nie dostaje** | **SHIP-BLOCKER** |
| A4-12 | KRYT | config.py:104 + summarizer.py:212-411 + transcriber.py:2394,2466 | Gęsty transkrypt (>2,0 zn/tok) → 400 „prompt too long" → fallback `transcript[:200]`, a frontmatter zapisuje **`summary_coverage: 0.889`, gdy API nie przeczytało ani znaku**; poprawka jednoliniowa (`is_fallback_summary` używany 24 linie niżej) | **SHIP-BLOCKER** |
| A4-10 | WYS | scheduler.py:39 vs synthesis.py:323 | Onboarding-digest na Sonnecie, tygodniowy przez `resolve_model` → **Haiku na zawsze** — produkt demonstruje się lepszym modelem, niż dostarcza | FIX-PRZED-H1 |
| A4-11 | WYS | config.py:225 | `VERDICT_ENABLED=False` w buildzie wysyłkowym — tygodniowy digest bez weryfikacji połączeń | FIX-PRZED-H1 |
| A4-2 | WYS | scheduler.py:203-215,338 | Korupcja stanu nieodróżnialna od świeżej instalacji → nieproszony płatny digest onboardingowy | FIX-PRZED-H1 |
| A4-3 | WYS | scheduler.py:271-279 | Adopcja tombstone'ów zakłada jednego producenta — fałszywe (2 procesy) | FIX-PRZED-H1 |
| A4-15 | WYS | tag_index.py:25,120,133 | Obrona przed samozatruciem = podwójna CZARNA lista (glob niererkursywny + 3 typy ręcznie); klasa błędu niezamknięta | FIX-PRZED-H1 |
| A4-16 | WYS | transcriber→tagger→tag_index | Pętla tagów bez tłumienia: zmyślony tag zyskuje df → wyższą pozycję w prompcie → większą szansę powtórki | FIX-PRZED-H1 |
| A4-17 | WYS | recall/vector_store.py:59 | sqlite bez WAL/busy_timeout, RLock tylko in-process — 2 procesy piszą do tej samej bazy → `database is locked` + ryzyko korupcji | FIX-PRZED-H1 |
| A4-18 | WYS | usage_ledger.py:167-176 | RMW godzin AI pod lockiem wątkowym — 2 procesy gubią inkrementy → **user dostaje godziny za darmo** (jednostka, w której sprzedawany jest PRO) | FIX-PRZED-H1 |
| A4-19 | WYS | config.py:213 + insight_metrics | W buildzie wysyłkowym **nikt nie mierzy pieniędzy** (metrics OFF); model kosztowy stoi na kohorcie testerów na najdroższym profilu | FIX-PRZED-H1 |
| A4-14 | ŚRE | transcriber.py:1678 | Wywołanie zafakturowane, padłe po inferencji (timeout/parse) → `last_usage=None` → niezliczone | FIX-PRZED-H1 |
| A4-4..8,13,20 | ŚRE/NIS | — | pełny parse JSON co 30 s × 2 procesy; seen-set O(n²); cooldown bramki tylko w pamięci; `_is_permanent_api_error` prywatny w 6 modułach; verdict fail-open znika z rachunku | DŁUG-PO-H1 |

**WERDYKT TEZY: potwierdzona — refaktor mechanizmu, nie łatki.** Dowód
strukturalny: 14 odpowiedzialności; macierz 8 pisarzy × 9 pól = **72 komórki,
7 różnych dyscyplin merge'u**, pokryte testami 19/72; 12/12 commitów
naprawczych wewnątrz `class DigestScheduler`; docstring sam przyznaje metodę
„hartowanie per wywołanie zamiast per pole". Rekomendacja: NIE dopisywać reguły
dla `new_notes` (20. komórka z 72) — zamknąć zapis stanu w istniejącym
`flock` + rozdzielić trwałość od polityki. Ten sam wzorzec (RMW bez blokady
międzyprocesowej) niezależnie w `usage_ledger` i `recall/vector_store`:
**apka działa w dwóch procesach, warstwa trwałości pisana jak dla jednego.**

## Weryfikacja adwersaryjna — V2 (pipeline) ✅

| ID | Werdykt | Klasa końcowa |
|---|---|---|
| A2-01 | CONFIRMED — zero guardów; aplikacja wszędzie poza reconcile ZAKŁADA podfoldery (`menu_app:1832` rglob, `obsidian_link:49` rglob) | **SHIP-BLOCKER** |
| A2-02 | CONFIRMED fakt / CORRECTED klasa — retencja jest nośna funkcjonalnie („Retranscribe file…", recovery bez dyktafonu); to nieudeklarowane archiwum bez polityki, nie wyciek | HIGH / FIX-PRZED-H1 |
| A2-03 | CONFIRMED / skutek skorygowany: fragment dostaje własny fingerprint → **duplikat + cichy fragment** (nie „fragment zamiast noty") | FIX-PRZED-H1 |
| A2-04 | CONFIRMED, SZERZEJ: TXT zostaje, sidecar go posiada → pętla płatna co 30 s; ostrzeżenie 80% budżetu **nie zapala się** (licznik tylko po sukcesie); do 120 obiegów/h × $0,16–0,31 | granica SHIP-BLOCKER |
| A2-08 | CONFIRMED + eskalacja MED→**HIGH**: trigger to każdy przejściowy OSError z iCloud (eviction), naprawa = ten sam płaski glob co A2-01 | FIX-PRZED-H1 |
| A2-06 | CONFIRMED (fallback bez `_owns_transcript`) | FIX-PRZED-H1 |

Bonus V2: siatka „indeks pusty a noty są" jest MARTWA — `transcriber.py:339`
woła `migrate_to_v2_index.py`, który wychodzi natychmiast po fladze
`index_migrated`; nawet działając, skanuje płasko.
Luka testowa: 4 testy reconcile, wszystkie na płaskim katalogu.

## Weryfikacja adwersaryjna — V3 (setup/UI) ✅

| ID | Werdykt | Klasa końcowa |
|---|---|---|
| A3-1 encoder | CONFIRMED, MOCNIEJ: 4 wejścia short-circuitują na `exists()`; repair NIE obejmuje encodera; w całym src/ **nie ma ani jednego rmtree** — uszkodzonego `.mlmodelc` nic nie umie skasować; komunikat każe zrobić operację będącą no-opem | **SHIP-BLOCKER** |
| A3-8 FDA | CORRECTED ↓MED: `exists()` nie kłamie pod TCC (PermissionError→False); ekran PERMISSIONS jest informacyjny (ma „Skip"); ekran Voice Memos daje drugą, opartą na realnej próbie ścieżkę do FDA. Realna wada: sonda mierzy nie te katalogi, których apka potrzebuje | FIX-PRZED-H1 (MED) |
| A5-1/2/3 | Mechanizm CONFIRMED (`save()` = cały `to_dict()`, docstring `mutate()` sam przyznaje dług), ale scenariusz „drugi płatny import" **REFUTED** — `pending_import_dir` kasowany jako pierwsza instrukcja przez `mutate`, okno = milisekundy; „odblokowany dysk" → wpis usunięty = dysk pyta ponownie (strata decyzji, nie cichy koszt); dashboard:4075 to wąski TOCTOU (LOW). Zostają HIGH: settings_window:1010/1040, menu_app:1055→1156, 2602→2618 | HIGH / FIX-PRZED-H1 |
| A5-6 handoff | CONFIRMED + gorzej: `HandoffResult.ok` istnieje i NIE jest czytany nigdzie; clipboard nie umie zgłosić porażki na żadnym z 2 poziomów. Bez utraty danych/kosztu → HIGH (insight opuszcza „Nowe" po fałszywym sukcesie) | HIGH / FIX-PRZED-H1 |
| A5-4 import | REFUTED dla audio (picker = 1 plik), CONFIRMED↑HIGH dla transkryptów: multi-select bez capa, zero słowa o koszcie — asymetria z wizardem, który cenę podaje i ostrzega przy >200 | HIGH / FIX-PRZED-H1 |

## Weryfikacja adwersaryjna — V1 (stan/koszty) ✅

| ID | Werdykt | Klasa końcowa |
|---|---|---|
| A1-20 | CORRECTED ↓: właściwy korzeń to osierocony wpis fingerprintu (nie prefiltr po nazwie); ratunek istnieje — „Retranscribe file…" ze stagingu albo restart — ale tester o nim nie wie (0 wzmianek w onboardingu) | FIX-PRZED-H1 |
| A4-1 | REFUTED jako blocker H1: mechanizm realny, ale `get_scheduler()` = singleton, LaunchAgent odpala 1 bundle → w wysyłce nie ma drugiego uchwytu (jedyny: `magic_digest.py` na dev-maszynie). Testy dwóch uchwytów: 15 dla innych pól, dla `new_notes` zero | DŁUG-PO-H1 |
| A1-11/12 | CONFIRMED: zero locka wątkowego w schedulerze; DeepScan ∥ PeriodicChecker/VoiceMemos w jednym procesie — realny wyścig, koszt trafienia = drugi płatny digest | FIX-PRZED-H1 |
| A4-12 | CORRECTED ↓: kłamiący coverage tylko dla transkryptu >400k znaków (~7 h) przy gęstości <2,05 zn/tok — najdłuższa nota w vaultcie ma 182k. Fix jednoliniowy + klasyfikacja 400 jako permanentnego | FIX-PRZED-H1 |
| A4-9 | REFUTED dla H1: `release-tester` piecze `TimshelTesterBuild` → `tester_mode=True` — pomiary H1 opisują wysyłany build. Znalezisko przenosi się na paczkę **GA**. **NOWE przy okazji:** adopcja flagi odpala się tylko gdy config nie ma klucza `tester_mode` — tester z wcześniejszym buildem dostanie instrumentację WYŁĄCZONĄ (puste metryki) | FIX-PRZED-GA + nowy wątek FIX-PRZED-H1 |
| A1-9 | CORRECTED: rekorder złapany fallbackiem po `source:`; NIE łapie Voice Memos (ciche `.v2`). **NOWE, ostrzejsze:** `VaultIndex.load()` po OSError (eviction iCloud) **sam nadpisuje indeks pustką** — jednoliniowy fix przed H1; koordynacja multi-Mac jako całość = dług | FIX-PRZED-H1 (nowa linia) + DŁUG-PO-H1 |
| A1-1 | CONFIRMED bez zastrzeżeń (konwencja tmp+replace istnieje w repo — settings jej nie stosuje; zero logu, zero backupu) | FIX-PRZED-H1 |

## A4b — Kontrakt kanałów candidate_assembly (sonnet) ✅

9 znalezisk: **2 FIX-PRZED-H1, 7 DŁUG-PO-H1**, zero blockerów (kanały failują soft).

| ID | Sev | Co | Klasa |
|---|---|---|---|
| A4b-1 | WYS | tester_mode włącza 4 kanały → protected ~39 przy capie 25 → **tag/bm25 (jedyne kanały zwalidowane end-to-end) tracą WSZYSTKIE sloty** w tygodniowym digeście kohorty H1 — regresja jakości, nie awaria | FIX-PRZED-H1 |
| A4b-2 | ŚRE | Log assembly liczy kanały PRZED capem i nie ma pola tag/bm25 — A4b-1 strukturalnie niewidoczny w telemetrii | FIX-PRZED-H1 |
| A4b-3..9 | ŚRE/NIS | Kaskada exclude kradnie pary kanałowi stance (błędna atrybucja); stopwords detekcji języka reużyte w scoringu bez testu wiążącego; prywatne symbole jako API 3 modułów; niespójny wzorzec przerywania cykli (deadlocku brak); graph bez cache (perf harnessu); latentna gałąź bez capu; dense bez progu podobieństwa | DŁUG-PO-H1 |

**Diagnoza „tier strukturalny Stanowisk 0/4"** (zaległość ze STATE): złożenie
trzech mechanizmów — sekcja opcjonalna z definicji (prompt: „most recordings
contain ZERO stances"), podwójne trafienie na małej próbie (okno 3–8 vs 15),
oraz **rozjazd tekstów**: `parse_stances` czyta `summary_md` ucięty ślepo do
2400 zn., a Stanowiska są TRZECIĄ sekcją — po PR #100 bogate podsumowania
wypychają je poza cięcie, mimo że `synthesis_md` chroni je priorytetowo.
Pomiar rozstrzygający: odsetek notatek ze Stanowiskami w tekście po cięciu vs
w surowym summary.

---

# SYNTEZA — bramka wyjścia fazy A

## Klasyfikacja końcowa (po weryfikacji adwersaryjnej)

**SHIP-BLOCKERY (2 + 1 na granicy):**
1. **A2-01** — reconcile z płaskim globem kasuje indeks przy notach w
   podfolderach → masowa retranskrypcja + koszty przy każdym starcie.
   (Reszta aplikacji skanuje rekurencyjnie — reconcile jest wyjątkiem.)
2. **A3-1** — pół-rozpakowany encoder Core ML na zawsze „zainstalowany";
   żadna ścieżka w aplikacji nie umie go naprawić (w src/ nie ma ani jednego
   `rmtree`); transkrypcja trwale martwa, komunikat każe zrobić no-op.
3. **A2-04 (granica)** — porażka zapisu noty (np. pełny dysk — który staging
   A2-02 sam zapełnia) → płatne summary co 30 s, niewidoczne dla licznika
   budżetu. Złożenie A2-02+A2-04 czyni to realnym w 3 tygodnie.

**FIX-PRZED-H1 (~24, najważniejsze wiązki):**
- **Atomowość/uczciwość stanu:** zapis config.json (A1-1) + `VaultIndex.load()`
  nadpisujący indeks pustką po OSError (nowe z V1) + wymuszenie `mutate()`
  w 3 call site'ach UI (A5-1/2/3 po korekcie) — jeden wątek tematyczny.
- **RLock w DigestScheduler** (A1-11/12) — zamyka podwójne płatności digestu.
- **Pętla kosztów i uczciwość noty:** A2-03 (obcięty staging), A2-05 (wyrzucone
  opłacone summary), A2-08 (index po błędzie odczytu), A2-06 (adopcja stale),
  A4-12 (coverage kłamie >7h — jednoliniowe), A4-14 (niezliczone wywołanie).
- **Setup:** A3-2 (repair nie weryfikuje ffmpeg/modelu), A3-6 (resume omija
  DOWNLOAD), A3-4 (fałszywe „no internet" za VPN), A3-5 (URL-e za redirectem),
  A3-8 (sonda FDA mierzy złe katalogi — po degradacji MED).
- **Kohorta H1 mierzy się poprawnie:** adopcja `tester_mode` u testera ze
  starym buildem (nowe z V1 — do weryfikacji na maszynie testera), A4b-1+A4b-2
  (kanały tester_mode głodzą zwalidowane tag/bm25 i log tego nie pokaże),
  A6-5 (martwy klucz = cichy skip), A6-6/7 (testy integracyjne bez asercji
  w CI), A6-8 (downloader bez pokrycia retry), A6-9 (recall wektorowy nigdy
  nie testowany na CI), A6-1 (mypy per-error-code zamiast ignore_errors).
- **UI:** A5-6 (handoff raportuje sukces przy porażce), A5-4 (import
  transkryptów bez wyceny), A5-10 (alert bez main-thread hop).
- **Model tygodniowy:** A4-10 (onboarding Sonnet vs tygodniówka Haiku) +
  A4-11 (verdict OFF w GA) — decyzja produktowa, nie tylko fix.

**FIX-PRZED-GA (nie blokuje H1):** A4-9 (tester_mode ≠ build GA — pomiary H1
nie przenoszą się na GA), A4-19 (w GA nikt nie mierzy pieniędzy).

**DŁUG-PO-H1 (~35):** pełna lista w sekcjach torów.

## Liczniki per podsystem i werdykt strukturalny

| Tor | Znaleziska | Po weryfikacji najcięższe |
|---|---|---|
| A1 stan | 20 | 0 blockerów, 12 fix |
| A2 pipeline | 21 | 2 blockery + 1 granica |
| A3 setup | 11 | 1 blocker, 6 fix |
| A4+A4b AI/connections | 29 | 0 blockerów H1, ~12 fix |
| A5 UI | 12 | 0 blockerów po korekcie, 5 fix |
| A6 bramki | 12 | 0 blockerów, 6 fix |

Zgodnie z dyscypliną z pamięci projektu — liczone per MECHANIZM, nie per tor.
Cztery wzorce przecinają tory i zbierają większość znalezisk:

1. **Warstwa trwałości pisana jak dla jednego procesu/wątku** (scheduler,
   usage_ledger, vector_store, settings, coreml_status) — werdykt A4:
   refaktor mechanizmu (RMW pod istniejącym flockiem, stan oddzielony od
   polityki), nie 20. komórka macierzy 8 pisarzy × 9 pól.
2. **Każdy czytelnik stanu połyka błąd i zwraca default** (7 plików stanu;
   najgroźniejsza instancja: nadpisanie indeksu pustką). Korupcja nigdy nie
   ujawnia się jako błąd — wraca jako objaw w innej warstwie. To jest
   mechanizm „kręcenia się w kółko" z PR #104/#106.
3. **`exists()` jako dowód poprawności** (encoder A3-1; klasa znana z #106 —
   „istnienie pliku nie dowodzi zapisu" — niedomknięta w setupie).
4. **Bramki przechodzą zielono, nic nie weryfikując** (klucz 401→skip, testy
   bez asercji, 22 testy recall nigdy nie biegną, mypy ignore_errors).

**Werdykt tezy o schedulerze:** potwierdzona (macierz 72 komórek, 7 dyscyplin
merge, 12/12 commitów naprawczych w protokole stanu) — refaktor mechanizmu.

## Rekomendowany zakres fazy B (do zatwierdzenia)

B1 (pewniaki z planu): bump beta.18 + tag + CHANGELOG; `make clean` poza dist/
+ `codesign --verify` w smoke; privacy copy; pin sqlite-vec; URL-e deps.
B2 (blockery): A2-01 (rekurencyjny reconcile + sanity-limit kasowań),
A3-1 (tmp+rename przy extractall, weryfikacja encodera, ścieżka naprawy),
A2-04+A2-02 (backoff po porażce postprocessu + polityka retencji stagingu).
B3 (wiązka atomowości): config.json atomowo, VaultIndex bez zapisu pustki,
`mutate()` w 3 call site'ach, RLock schedulera.
B4 (uczciwość pomiaru H1): adopcja tester_mode, A4b-1/2, e2e gate, markery
integration, mypy per-error-code.
B5 (decyzje produktowe, osobno): wycena importu, retencja stagingu jako
funkcja („archiwum nagrań").
ROZSTRZYGNIĘTE (Radek, 2026-08-07): model digestu — pierwszy (onboarding) na
Sonnecie, każdy następny na **Opusie 5 z verdictem**, zgodnie z wiążącą decyzją
kosztową z 2026-08-06; naprawa A4-10/A4-11 = przywrócenie tego założenia jako
domyślki produktu, nie tylko tester_mode.

- A2 ścieżka krytyczna (opus) — w toku
- A3 setup/downloader (sonnet) — w toku
- A4 AI/connections (opus) — w toku
- A5 UI (sonnet) — w toku
- A6 bramki CI/testów (sonnet) — w toku
