# Production dependencies
MacFSEvents>=0.8.4
pyobjc-framework-Cocoa>=10.0
pyobjc-framework-FSEvents>=10.0

# Transcription engine
# whisper.cpp is installed separately via scripts/install_whisper_cpp.sh
# No Python dependencies required for whisper.cpp

# Logging and utilities
python-dotenv>=1.0.0
click>=8.1.0

# macOS Menu Bar App
rumps>=0.4.0
# Renders the menu-bar wave sigil (src/menu_app.py) — must ship in the bundle
Pillow>=10.0

# LLM/Summarization
# >=0.40 for native structured outputs (Pydantic response_format) used by the
# connection-synthesis layer (src/connections/synthesis.py).
anthropic>=0.40.0

# HTTP client for downloads
httpx>=0.27.0

# Audio metadata
mutagen>=1.47.0

